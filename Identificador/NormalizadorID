
public class NormalizadorID{
    public static void main(String[] args) {
      String sucio =" user_123 ";
    String limpio = sucio.trim().toUpperCase();
    System.out.println("user_123: " + sucio + " " );
    System.out.println("limpiar: " + limpio + " ");

     System.out.println(" user_123  " + System.identityHashCode(sucio));
    System.out.prinln( "limpiar " + System.identityHashCode(limpio));
    StringBuilder sb = new StringBuilder(sucio);
    }

      

}
